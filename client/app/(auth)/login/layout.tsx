import React from "react";

export const metadata = {
  title: "Login to Connecto",
};
const layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default layout;
