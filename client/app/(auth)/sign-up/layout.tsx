import React from "react";

export const metadata = {
  title: "Sign up for Connecto",
};
const layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default layout;
